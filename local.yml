---
- hosts: localhost
  connection: local
  become: true

  tasks:
    - name: install neofetch
      package:
        name:
          - neofetch
          - htop
          - tmux
          - docker
